from pipelines.classify import classify_sector

print(
    classify_sector(
        "Proyecto sobre explotación minera en Colombia",
        "Regula las actividades de empresas mineras en zonas protegidas"
    )
)
